name: Daily Job

on:
  schedule:
    - cron: '0 9 * * 1-5'

jobs:
  run-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy and run Fly.io job
      run: |
        fly deploy
        fly machines run produzir-substack
